# Example environment variables for FairShare
# Copy this file to `.env` and update the values for your environment.

# Publicly accessible URL for the app (used to construct invite/reset links).
# In production set this to https://your-domain.com
APP_URL=http://localhost:3000

# Secret used to sign JWT session cookies. MUST be changed in production.
JWT_SECRET=change-this-to-a-strong-random-secret

# Node environment
NODE_ENV=development

# Dev server port (used by Vite/Next when running locally)
PORT=3000
PREVIEW_PORT=4173
